app: apihydra
kind: steps
spec:
  steps:
    - request:
        path: /zero-types
        body: |
          {
            "request": true
          }
        defaults:
          headers:
            Authorization: Bearer complete-debug-secret
            Cookie: session=unredacted-cookie
      response:
        expected_body: '{"version":0}'
      debug: true
    - request:
        path: /after-debug
