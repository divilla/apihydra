app: apihydra
kind: steps
spec:
  steps:
    - request:
        path: /debug-invalid-request
        body: '@/etc/passwd'
      response:
        expected_body: '{"id":7,"ok":true}'
        expected_types:
          ".[": [string]
      debug: true
